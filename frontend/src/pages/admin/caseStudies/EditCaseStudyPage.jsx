import { useEffect,useState } from "react";import toast from "react-hot-toast";import { useNavigate,useParams } from "react-router-dom";import AdminPageHeader from "../../../components/admin/layout/AdminPageHeader.jsx";import CaseStudyForm from "../../../components/admin/forms/CaseStudyForm.jsx";import { caseStudyService } from "../../../services/caseStudyService.js";import { getErrorMessage } from "../../../utils/getErrorMessage.js";
export default function EditCaseStudyPage(){const{id}=useParams();const nav=useNavigate();const[item,setItem]=useState();const[busy,setBusy]=useState(false);useEffect(()=>{caseStudyService.getCaseStudy(id).then(({data})=>setItem(data.data)).catch(e=>toast.error(getErrorMessage(e)))},[id]);async function save(v){setBusy(true);try{await caseStudyService.updateCaseStudy(id,v);toast.success("Case study updated");nav(`/admin/case-studies/${id}`)}catch(e){toast.error(getErrorMessage(e))}finally{setBusy(false)}}return <div className="mx-auto max-w-5xl"><AdminPageHeader title="Edit case study"/>{item?<CaseStudyForm initialValue={item} onSubmit={save} submitting={busy}/>:<p>Loading...</p>}</div>}
