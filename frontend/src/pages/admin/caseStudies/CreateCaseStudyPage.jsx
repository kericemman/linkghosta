import { useState } from "react"; import toast from "react-hot-toast"; import { useNavigate } from "react-router-dom"; import AdminPageHeader from "../../../components/admin/layout/AdminPageHeader.jsx"; import CaseStudyForm from "../../../components/admin/forms/CaseStudyForm.jsx"; import { caseStudyService } from "../../../services/caseStudyService.js"; import { getErrorMessage } from "../../../utils/getErrorMessage.js";
export default function CreateCaseStudyPage(){const nav=useNavigate();const[busy,setBusy]=useState(false);async function save(v){setBusy(true);try{const{data}=await caseStudyService.createCaseStudy(v);toast.success("Case study created");nav(`/admin/case-studies/${data.data._id}`);}catch(e){toast.error(getErrorMessage(e));}finally{setBusy(false);}}return <div className="mx-auto max-w-5xl"><AdminPageHeader title="New case study" description="Turn a client result into clear, credible proof."/><CaseStudyForm onSubmit={save} submitting={busy}/></div>}
