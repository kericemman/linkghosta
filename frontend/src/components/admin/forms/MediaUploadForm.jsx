import{UploadCloud}from"lucide-react";import{useState}from"react";
export default function MediaUploadForm({onSubmit,submitting}){const[file,setFile]=useState();const[title,setTitle]=useState("");return <form onSubmit={e=>{e.preventDefault();const data=new FormData();data.append("file",file);data.append("title",title);onSubmit(data)}} className="space-y-5 rounded-lg border border-black/10 bg-white p-6"><label className="block text-sm font-bold">Asset title<input value={title} onChange={e=>setTitle(e.target.value)} className="mt-2 w-full rounded-md border border-neutral-300 px-3 py-2.5" placeholder="Founder portrait"/></label><label className="flex min-h-48 cursor-pointer flex-col items-center justify-center rounded-lg border-2 border-dashed border-brand-300 bg-brand-50 p-6 text-center"><UploadCloud size={34} className="text-brand-700"/><span className="mt-3 text-sm font-extrabold">{file?file.name:"Choose an image or file"}</span><span className="mt-1 text-xs text-neutral-500">Images up to 25 MB are compressed automatically</span><input required type="file" className="sr-only" onChange={e=>setFile(e.target.files[0])}/></label>{file?.type.startsWith("image/")&&<img src={URL.createObjectURL(file)} alt="Preview" className="max-h-72 w-full rounded-md object-contain bg-neutral-50"/>}<button disabled={submitting||!file} className="inline-flex items-center gap-2 rounded-md bg-brand-600 px-5 py-2.5 text-sm font-bold text-white disabled:opacity-50"><UploadCloud size={17}/>{submitting?"Compressing & uploading...":"Upload asset"}</button></form>}
